7:27: I need to understand the model layer. 10min reading, 10min wirte back, 10min confirm. 10min craft

The model layer is a set of files contain plain functions which handles business logics. It enables reusability, adds readability to the API layer.

It lives inside the convex folder, got bundled into the server, and got called by api functions just like how js funcaitons call other js functions. It has access to generated types, like Id<"">, Ctx, so that business logic can be write directly here.

the modle function describe what it needs, and a registererd function pass the 'cart' to it. 
